{
    "version": 2,
    "buildCommand": "export MONGODB_URI=${MONGODB_URI:-'mongodb+srv://demo:demo@cluster0.mongodb.net/demo?retryWrites=true&w=majority'} && npm install && npm run build",
    "installCommand": "npm install",
    "outputDirectory": ".next",
    "env": {
        "NODE_ENV": "production"
    }
}